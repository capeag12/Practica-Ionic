import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.practicaionic',
  appName: 'practicaionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
